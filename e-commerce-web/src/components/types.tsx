export type FormValues = {
  lastName: string;
  description: string;
  firstName: string;
  address: string;

  phoneNumber: number;
};

export type productItem = {
  productId: string;
  productCount: number;
  size: string;
  images: string[];
  price: number;
  productName: string;
};
